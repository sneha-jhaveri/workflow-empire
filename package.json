{
  "name": "workflow-empire",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.5.0",
    "pg": "^8.16.3",
    "pino-pretty": "^13.1.1",
    "prom-client": "^15.1.3",
    "supertest": "^7.1.4",
    "vitest": "^3.2.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/supertest": "^6.0.3"
  },
  "scripts": {
    "dev:api": "NODE_ENV=development PROCESS_ROLE=api tsx apps/backend/src/index.ts",
    "dev:worker": "NODE_ENV=development PROCESS_ROLE=worker tsx apps/backend/src/index.ts",
    "dev:web": "vite --host",
    "typecheck": "tsc -b",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "vitest"
  }
}
